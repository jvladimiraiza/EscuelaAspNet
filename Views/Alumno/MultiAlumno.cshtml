@* esto es un comentario *@
@model IEnumerable<Alumno>

@{
    ViewData["Title"] = "Informacion Alumno";
    // recibiendo data que viene del controller 
    // @Model.nombre
}
<div class="container">
    <h1> @ViewData["Title"] </h1>
        <a class="btn btn-success" asp-controller="Alumno"
            asp-action="Create">Nuevo
            <i class="icon-plus"></i>
        </a>
        <partial name="ListaObjetoEscuela"/>
    <p>@ViewData["Fecha"]</p>
</div>